<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Listview"
        tools:targetApi="31">
        <activity
            android:name="listview_nangcao.listview_nangcao"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="radio_button.radio_button"
            android:exported="true"></activity>
        <activity
            android:name="checkbox.checkbox"
            android:exported="false" />
        <activity
            android:name="switch_1.switch_activity"
            android:exported="true"></activity>
        <activity
            android:name="hinhanh.imageview"
            android:exported="true" />
        <activity
            android:name=".MainActivity"
            android:exported="true">


        </activity>
    </application>

</manifest>